package ro.polak.spring.datafixtures;

/** The fixtures will be loaded by the order of the types in this enum (based on the ordinal). */
public enum DataFixtureType {
  DICTIONARY,
  TEST,
  DEMO,
  PERFORMANCE;
}
